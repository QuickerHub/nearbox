appId: com.quickerhub.nearbox
productName: Nearbox
copyright: Copyright © QuickerHub
artifactName: Nearbox-${version}-${os}-${arch}.${ext}
directories:
  output: release
  buildResources: build
files:
  - out/**/*
extraResources:
  - from: resources
    to: .
    filter:
      - nearbox.apk
      - icons/**
      - cli/**
win:
  icon: build/icon.png
  target:
    - target: nsis
      arch:
        - x64
    - target: zip
      arch:
        - x64
  signAndEditExecutable: true
  # CI has no code-signing cert; do not pick a random Windows identity each run.
  signDlls: false
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  shortcutName: Nearbox
asar: true
